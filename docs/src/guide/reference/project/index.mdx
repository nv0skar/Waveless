# `project.toml` file

The file `project.toml` located in the root of the project's folder defines it's config. In this section, we will explore all the tunable aspects of the Waveless framework.

It's required to define a project's name in the TOML top level:

```toml
name = "project_name"
```
